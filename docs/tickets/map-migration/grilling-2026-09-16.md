# Map migration — grilling record, 16 September 2026

Input to `/to-spec`. Participants: product owner; Claude (Anthropic) as interviewer. Sources read first:
`CONTEXT.md`, `docs/epics/analytics-platform.md`, PRD §6.9 and §13, ADR-0009, ADR-0018, the map code under
`app/src/frontend/map/` and the tile server under `app/src/tiles/`.

## Decisions (each with its record)

| # | Decision | Record |
|---|---|---|
| 1 | Fresh edits: nightly PMTiles base plus an edits-since overlay from a read-only endpoint; no live tile server in V1. OD-5 closed. | ADR-0022 |
| 2 | One PMTiles archive, one `buildings` layer, every styled attribute plus `location_number`; one style config generates both MapLibre expressions and the legend. | ADR-0023 |
| 3 | Click resolves locally with `queryRenderedFeatures`; selection highlight via feature-state; the map no longer calls `/api/buildings/locate`. Multi-edit mode uses the same hit-test. | spec |
| 4 | One export job: `REPEATABLE READ` transaction, `now()` from it, one FlatGeobuf export, artefacts derived, manifest last; export role, generous timeout, `nice`/`ionice`, quiet window; no statement timeout. E0 delivers job + FlatGeobuf + PMTiles + manifest; M1 adds GeoParquet from the same FlatGeobuf. NFR-2.5 wording to be refined. | ADR-0024 |
| 5 | Tools: `ogr2ogr` → FlatGeobuf → tippecanoe (Felt) → PMTiles. Production GDAL version unknown; M1 needs GDAL ≥ 3.9. | ADR-0025, inventory |
| 6 | Basemap stays OSM raster in E0 (MapLibre raster source; night theme via raster paint properties). OSM host already sees zoom-19 requests, so FR-14.11.c applies today and the M5 notice names it; OSMF tile policy logged as inherited compliance. OD-11 opened for a self-hosted Protomaps Ghana extract by M4; preview with planners during soak. | PRD OD-11, inventory |
| 7 | Archive and manifest served by the existing Express static handler (Range, ETag, long Cache-Control); no CDN in E0. | spec |
| 8 | Flag: `MAP_STACK` env default injected via preloaded state; `?map=` override stored in a functional cookie (listed in the M5 notice); layer-options panel shows the active stack during soak; removal ticket exists, blocked on parity and soak. | spec; ticket 01 |
| 9 | React binding: `@vis.gl/react-maplibre`; verify React 17 / TypeScript 4.2 peers; fallback is a TypeScript upgrade, not a different binding. | ADR-0026, inventory |
| 10 | Two gates: parity (opt-in; owner plus two planners; one Android on mobile; full edit round-trip and geolocation) then soak (default flip, four weeks, benchmarks at flip); blocking vs cosmetic defects; owner sign-off with date and checklist version in the feature doc. | ADR-0027 |
| 11 | Benchmarks: scripted Playwright path; runs A (laptop), B (laptop, fixed throttle, judged), C (Android on mobile); bytes per segment beside timings; cold cache per repetition; same basemap; pass = after ≤ before on every metric in B; targets set from the baseline. | `docs/benchmarks/map-migration-method.md` |

**Correction, 2026-09-16 (during `/to-spec`).** Row 9 says the fallback if the peer check fails is "a TypeScript
upgrade". That is not what was decided. ADR-0026 (accepted) and the inventory row are the record: a thin component
wrapper over `maplibre-gl` with no framework upgrade in E0. The row is left as written; this note governs.

## Assumptions stated during the session (not separately decided)

- Feature slug `map-migration`; ticket paths `docs/tickets/map-migration/issues/NN-<slug>.md`.
- The Leaflet tree is left untouched behind the flag until ticket 01 removes it.
- Tile zoom range matches today: data from zoom 9, tippecanoe max zoom 16 with MapLibre overzoom to 19; exact settings benchmarked.
- The archive is built so `pmtiles extract` can clip a district for packs (FR-10.2) without a rebuild.
- Three themes (light, night, night_outlines) are three style variants from the one style config.
- Boundary layers, search pins and the geolocation control are ported to `@vis.gl/react-maplibre` with the same behaviour (zoom-dependent labels, permanent tooltips become symbol layers).
- The edits-since endpoint returns geometry and styled attributes only, capped, no contributor identity; it carries a short statement timeout like other interactive endpoints.
- The client fetches the manifest at page load and after each save to learn the archive's "as of".
- Only three code paths consume `/tiles/` today (`server.tsx`, the edit-service tile-cache expiry, `get-tile-layer-url.ts`); all go with ticket 01.

## Parity checklist seed (from the current code; the spec turns this into the checklist)

Category colouring for all 54 tilesets and their legends; legend title, description, disclaimer; theme
switch (light, night, night_outlines) including background colour; editable-buildings toggle; region and
district boundary layers with zoom-dependent labels (region ≤ 11, district ≥ 12); city boundary; number
labels from zoom 17; building selection and highlight in view, edit and multi-edit modes; full edit
round-trip including the change appearing on the map without reload and in the edit history; search box
results as pins with hover and select recentring; geolocation control (fix, accuracy circle, heading,
recentre zoom, error surfacing) with no network write; zoom control and attribution; minimum zoom 7,
maximum 19; initial viewport from `cc-config.json`; layer-options panel show and hide without resetting
selections; behaviour on an Android phone over mobile data.

## Test seams agreed for the spec (16 September 2026)

MapLibre needs WebGL, so nothing in the map itself runs under jest's jsdom. Pure logic runs under jest;
browser behaviour runs under Playwright; the parity checklist (ADR-0027) stays the human gate.

| # | Seam | Runner | What is asserted | Prior art |
|---|---|---|---|---|
| 1 | Style config: one function turns a category map definition into legend entries plus a MapLibre expression | jest | For all 54 definitions, every legend colour appears in the expression and every expression colour appears in the legend | land-use domain-logic test under `app/src/api/services/__tests__/` |
| 2 | Export job, run against the dev database | shell plus a small checker | The manifest "as of" equals the export transaction's recorded timestamp; the FlatGeobuf and the PMTiles archive both derive from that single read (same feature count and ids, same "as of" in the archive metadata); checksums recompute; archive property keys equal the style config's attribute list plus `location_number` | none; new seam |
| 3 | Edits-since service, data access mocked | jest | Row cap enforced and reported; timestamp validation; the boundary is inclusive (`>=`) at the manifest "as of", so an edit at the exact export instant is never lost; no username or contributor identity in the output | edit-history service test |
| 4 | Map-stack flag resolver: pure function of environment default, query parameter and cookie | jest | Environment default applies when nothing else is set; an explicit query parameter sets the cookie; cookie precedence over the environment default; server-rendered and client resolution agree for the same inputs | none; pure function |
| 5 | Browser smoke via Playwright against the dev server, as a differential test while both stacks coexist | Playwright | The same fixed path runs on both stacks via the flag: building id from the local hit-test (MapLibre) equals the id from `/api/buildings/locate` (Leaflet) at fixed coordinates; legend and colour state after each category switch match; on MapLibre an edit appears in the overlay without reload | none; shares the runner with the benchmark method |

Shared path definition: the smoke test and the benchmark method (`docs/benchmarks/map-migration-method.md`)
read one path file (viewport, two named places, zoom steps, category pair, fixed building), so the
differential test and the benchmark cannot drift apart.

## Corrections made by the product owner during the session

Recorded here so the reasoning behind ADR-0024, the OSM inventory entry and the benchmark method is traceable.

### cross-artefact consistency (P5)
- Pattern: proposed a PMTiles export job in isolation; user pointed out tiles and the GeoParquet snapshot
  must share one "as of" or a result view could highlight buildings the tiles do not show.
- Rule: before recommending any data artefact, check it against P1–P9 and every sibling artefact that a
  user could see side by side. Anything time-stamped must come from one transaction with siblings.

### statement timeout is not a bulk-export guard
- Pattern: suggested a statement timeout on the export role, echoing NFR-2.5's wording. User: it kills the
  export the day the data grows; intent is "never block contributors".
- Rule: read a requirement's intent, not its example mechanism. For bulk reads use a dedicated role,
  generous timeout, nice/ionice, quiet run window; keep short timeouts for interactive endpoints.

### inherited third-party data flows
- Pattern: claimed "no privacy-notice change in E0" because E0 adds no new host; user: the OSM tile host
  already sees zoom-19 requests, so FR-14.11.c applies today and E0 does not exempt it.
- Rule: when assessing a change's privacy impact, list every third-party host the baseline already contacts;
  "no new flow" is not "no disclosure needed". Record inherited services in the tool inventory.

### bandwidth is the binding constraint
- Pattern: proposed a latency-only benchmark; user: bandwidth, not latency, binds in Ghana; record bytes per step.
- Rule: any performance method for this project records bytes transferred beside every timing, includes a
  throttled reproducible run, and sets targets from a recorded baseline rather than a guess.

## Noted, not actioned

- `CLAUDE.md`, `CONTEXT.md`, `CODING_STANDARDS.md`, `AGENTS.md` and `tasks/` are untracked and listed in
  `.git/info/exclude`, while committed files (`docs/agents/domain.md`, the epic guide) refer to `CONTEXT.md`
  as the canonical glossary. A fresh clone will not have it. Product owner to decide.
- Branch naming resolved 2026-09-16: the integration branch stays `feature/maplibre-migration`, because
  the name says what the migration is to and "map migration" could mean anything in two years. The PRD
  (§6.9, §13) and the epic guide now name it; the ticket directory slug stays `map-migration`.
- ADR-0018 is accepted and was not edited; the ADR index row points to ADR-0027 instead.
