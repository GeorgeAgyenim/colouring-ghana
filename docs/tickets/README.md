# Tickets

Issues and PRDs for Colouring Ghana live here as markdown, one directory per feature:
`docs/tickets/<feature-slug>/PRD.md` and `docs/tickets/<feature-slug>/issues/<NN>-<slug>.md`.
Conventions, status values and branch naming: `docs/agents/issue-tracker.md` and `docs/agents/triage-labels.md`.
